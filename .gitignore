.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
*.iml
.idea/
out/

# macOS Finder metadata. Never useful in a repo, and this one is built and
# run on Windows - these only exist because it was browsed from a Mac.
.DS_Store

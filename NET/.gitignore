# .NET build output. Nothing under these is authored - all of it is
# regenerated by `dotnet build`, and the self-contained win-x64 publish alone
# is thousands of files and a couple of hundred megabytes.
bin/
obj/
publish/
TestResults/
